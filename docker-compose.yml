version: '3'
services:
  spring-app:
    container_name: toeicute-service
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8081:8080"
    environment:
      - DB_HOST=192.168.1.42
      - DB_PORT=3300
      - DB_USERNAME=root
      - DB_PASSWORD=1234
